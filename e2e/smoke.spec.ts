import { test,expect } from "@playwright/test";test("login protege o painel e não há overflow",async({page})=>{await page.goto("/dashboard");await expect(page).toHaveURL(/\/login/);await page.getByLabel("Email").fill(process.env.CATWORLD_ADMIN_EMAIL??"admin@example.com");await page.getByLabel("Senha").fill(process.env.CATWORLD_ADMIN_PASSWORD??"ChangeMe123!");await page.getByRole("button",{name:"Entrar"}).click();await expect(page).toHaveURL(/\/dashboard/);const dimensions=await page.evaluate(()=>({client:document.documentElement.clientWidth,scroll:document.documentElement.scrollWidth}));expect(dimensions.scroll).toBe(dimensions.client)});