import { prisma } from "@/server/db";import { PageHeader,Panel,StatusBadge } from "@/components/ui/primitives";import { CreateUserDialog,EditUserDialog } from "@/components/management/user-dialogs";import { ManageGrantsDialog } from "@/components/management/manage-grants-dialog";import { resolveActor,requireRole } from "@/server/auth/actor";export const dynamic="force-dynamic";
export default async function UsersPage(){requireRole(await resolveActor(),["ADMIN"]);const rows=await prisma.user.findMany({orderBy:{name:"asc"}});return <div className="space-y-6"><PageHeader eyebrow="Equipe" title="Usuários da plataforma" description="Contas administrativas e analíticas do Catworld." actions={<CreateUserDialog/>}/><Panel><div className="overflow-x-auto"><table className="table"><thead><tr><th>Nome</th><th>Email</th><th>Papel</th><th>Status</th><th/></tr></thead><tbody>{rows.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td><StatusBadge status={u.active?"healthy":"inactive"}/></td><td><div className="flex justify-end gap-1"><ManageGrantsDialog userId={u.id} userName={u.name}/><EditUserDialog id={u.id} name={u.name} role={u.role} active={u.active}/></div></td></tr>)}</tbody></table></div></Panel></div>}