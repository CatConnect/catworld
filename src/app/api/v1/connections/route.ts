import type { NextRequest } from "next/server";import { z } from "zod";import { prisma } from "@/server/db";import { resolveActor,requireRole } from "@/server/auth/actor";import { encryptSecret } from "@/server/security/crypto";import { handleApiError,ok } from "@/server/http";
export async function GET(r:NextRequest){try{const a=await resolveActor(r);requireRole(a,["ADMIN"]);const rows=await prisma.connection.findMany({orderBy:{createdAt:"desc"},select:{id:true,name:true,environment:true,server:true,databaseName:true,username:true,active:true,lastStatus:true,lastLatencyMs:true,lastCheckedAt:true,createdAt:true,updatedAt:true}});return ok(rows);}catch(e){return handleApiError(e)}}
export async function POST(r:NextRequest){try{const a=await resolveActor(r);requireRole(a,["ADMIN"]);const i=z.object({name:z.string().min(2),environment:z.enum(["Produção","Homologação","Desenvolvimento"]),server:z.string().min(3),databaseName:z.string().min(1),username:z.string().min(1),password:z.string().min(1)}).parse(await r.json());const {password,...visible}=i;return ok(await prisma.connection.create({data:{...visible,encryptedCredentials:encryptSecret(JSON.stringify({password}))}}),undefined,201);}catch(e){return handleApiError(e)}}