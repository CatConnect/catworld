import type { NextRequest } from "next/server";import { prisma } from "@/server/db";import { resolveActor,requireRole } from "@/server/auth/actor";import { handleApiError,ok } from "@/server/http";
export async function GET(r:NextRequest){try{const a=await resolveActor(r);requireRole(a,["ADMIN","DATA_MANAGER"]);const cursor=r.nextUrl.searchParams.get("cursor")??undefined;const rows=await prisma.auditEvent.findMany({take:101,...(cursor?{skip:1,cursor:{id:cursor}}:{}),orderBy:{createdAt:"desc"},include:{user:{select:{name:true,email:true}}}});const hasMore=rows.length>100,data=hasMore?rows.slice(0,100):rows;return ok(data,{nextCursor:hasMore?data.at(-1)?.id:null});}catch(e){return handleApiError(e)}}