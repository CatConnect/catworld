import type { NextRequest } from "next/server"; import { z } from "zod"; import { resolveActor } from "@/server/auth/actor"; import { syncActorGrants } from "@/server/auth/sync-grants"; import { executeReadOnly } from "@/server/azure/sql"; import { handleApiError,ok } from "@/server/http"; import { audit } from "@/server/audit";
export async function POST(request:NextRequest){try{const actor=await resolveActor(request);const input=z.object({sql:z.string().min(1).max(50000),timeout:z.number().int().min(1).max(120).default(30),limit:z.number().int().min(1).max(10000).default(10000)}).parse(await request.json());await syncActorGrants(actor);const result=await executeReadOnly(actor.principal,input.sql,input.timeout,input.limit);await audit(actor,"QUERY_EXECUTED","query",undefined,{rowCount:result.rowCount,executionTimeMs:result.executionTimeMs});return ok(result);}catch(e){return handleApiError(e)}}