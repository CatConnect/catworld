"use client";
import { useState } from "react";import { signIn } from "next-auth/react";import { Database,LogIn } from "lucide-react";import { useRouter } from "next/navigation";
export default function LoginPage(){const router=useRouter(),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setError("");const form=new FormData(event.currentTarget);const result=await signIn("credentials",{email:form.get("email"),password:form.get("password"),redirect:false});setLoading(false);if(result?.error)setError("Email ou senha inválidos");else{router.push("/dashboard");router.refresh()}}return <main className="surface-grid grid min-h-screen place-items-center bg-base-200 p-5"><div className="w-full max-w-md rounded-box border border-base-300 bg-base-100 p-7 shadow-xl"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-xl bg-primary text-primary-content"><Database/></span><div><h1 className="text-xl font-bold">Catworld</h1><p className="text-xs text-base-content/50">Seu data lake, sem labirinto.</p></div></div><form onSubmit={submit} className="mt-8 space-y-4"><label className="fieldset"><span className="fieldset-legend">Email</span><input className="input w-full" name="email" type="email" required autoComplete="email"/></label><label className="fieldset"><span className="fieldset-legend">Senha</span><input className="input w-full" name="password" type="password" required minLength={8} autoComplete="current-password"/></label>{error&&<div className="alert alert-error alert-soft py-2 text-sm">{error}</div>}<button disabled={loading} className="btn btn-primary w-full"><LogIn size={17}/>{loading?"Entrando...":"Entrar"}</button></form></div></main>}