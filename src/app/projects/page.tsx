import Link from "next/link";import { Database,Table2 } from "lucide-react";import { prisma } from "@/server/db";import { PageHeader,Panel } from "@/components/ui/primitives";import { CreateCatalogDialog } from "@/components/management/create-catalog-dialog";import { EditCatalogDialog } from "@/components/management/edit-catalog-dialog";import { resolveActor } from "@/server/auth/actor";import { visibleProjectIds } from "@/server/auth/permissions";export const dynamic="force-dynamic";
export default async function ProjectsPage(){const actor=await resolveActor(),ids=await visibleProjectIds(actor);const projects=await prisma.project.findMany({where:{active:true,...(ids?{id:{in:ids}}:{})},include:{datasets:{where:{active:true},include:{tables:true}}},orderBy:{name:"asc"}});return <div className="space-y-6"><PageHeader eyebrow="Catálogo" title="Projetos" description="Organize datasets, acessos e integrações em espaços isolados." actions={<CreateCatalogDialog kind="project"/>}/><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{projects.map(p=>{const dsCount=p.datasets.length,tbCount=p.datasets.reduce((n,d)=>n+d.tables.length,0);return <Panel key={p.id} className="group relative transition-shadow hover:shadow-md"><Link href={`/projects/${p.slug}`} className="absolute inset-0 z-0 rounded-box" aria-label={`Abrir projeto ${p.name}`}/><div className="relative z-10 p-5"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><h2 className="truncate text-lg font-semibold">{p.name}</h2><p className="mt-1 line-clamp-2 min-h-10 text-sm text-base-content/55">{p.description||<span className="italic opacity-60">Sem descrição</span>}</p></div><div className="shrink-0"><EditCatalogDialog kind="project" id={p.id} name={p.name} description={p.description} active={p.active}/></div></div><div className="mt-5 flex gap-3"><div className="flex flex-1 items-center gap-2.5 rounded-xl bg-base-200 p-3.5"><div className="rounded-lg bg-primary/10 p-1.5 text-primary"><Database size={15}/></div><div><p className="text-lg font-bold leading-none">{dsCount}</p><p className="mt-0.5 text-xs text-base-content/50">{dsCount===1?"dataset":"datasets"}</p></div></div><div className="flex flex-1 items-center gap-2.5 rounded-xl bg-base-200 p-3.5"><div className="rounded-lg bg-secondary/10 p-1.5 text-secondary"><Table2 size={15}/></div><div><p className="text-lg font-bold leading-none">{tbCount}</p><p className="mt-0.5 text-xs text-base-content/50">{tbCount===1?"tabela":"tabelas"}</p></div></div></div></div></Panel>})}</div></div>}